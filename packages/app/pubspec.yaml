name: app
description: Entrypoint, composición de DI, router y shell responsive.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  core:
    path: ../core
  design_system:
    path: ../design_system
  feature_catalog:
    path: ../features/feature_catalog
  flutter_riverpod: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0

flutter:
  uses-material-design: true
